include ../mixins/all
doctype html
html
  head
    include _head

  body(class=bodyClass)
    .wrapper
      .maincontent
        block content
        

    //LOAD SCRIPTS
    block scripts
      script(type="text/javascript" src="js/app.js")
